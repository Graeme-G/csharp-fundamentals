# Module 1: C# Fundamentals

## Overview

This module builds the foundation everything else depends on. Starting from zero — what .NET is, how to create and run a program, and working through the core language building blocks: types, variables, operators, control flow, loops, strings, methods, and arrays.

By the end of this module you will be able to read and write basic C# programs with confidence and have a solid mental model of how the language works before moving into object-oriented programming.

This module is a **git repository**. Initialise it before starting workshop 1.

---

## Workshops

| # | Workshop | Description | Status |
|---|----------|-------------|--------|
| 1 | `01-your-first-csharp-program` | What .NET is, creating a console project, project structure, top-level statements, Console.WriteLine/ReadLine, running your program | Not Started |
| 2 | `02-types-and-variables` | Built-in value types (int, double, decimal, char, bool), string as a reference type, declaring and assigning variables, var and type inference, const | Not Started |
| 3 | `03-working-with-numbers` | Arithmetic operators, integer vs floating point division, when to use decimal vs double, implicit and explicit type conversions, casting, the Math class | Not Started |
| 4 | `04-booleans-and-comparisons` | The bool type, comparison operators, logical operators (&&, \|\|, !), short-circuit evaluation, operator precedence | Not Started |
| 5 | `05-control-flow` | if/else if/else, switch statement, switch expression, ternary operator | Not Started |
| 6 | `06-loops` | for, while, do-while, foreach, break and continue, nested loops | Not Started |
| 7 | `07-strings` | String literals, interpolation, verbatim strings, immutability, common string methods (Length, ToUpper, Contains, Replace, Trim, Split, Substring), string comparison | Not Started |
| 8 | `08-methods` | Declaring methods, return types, void, parameters, calling methods, overloading, optional parameters, named arguments, expression-bodied methods | Not Started |
| 9 | `09-arrays` | Declaring and initialising arrays, accessing by index, Length, iterating with for and foreach, Array class methods, index out of bounds | Not Started |

---

## Notes

- Each workshop is self-contained and builds on the previous
- Workshop directories and code are created as each workshop is started
- See `PROGRESS.md` for current status and session notes
